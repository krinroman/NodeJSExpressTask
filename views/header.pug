header
      .row.header-wrapper
        .col-8.menu-block
          nav.nav.nav-block
            if page == "list"
              a.nav-item.active-item(id="button_list" href="/") Список
            else
              a.nav-item(id="button_list" href="/") Список
            if page == "lk"
              a.nav-item.active-item(id="button_lk" href="/lk") Личный кабинет
            else
              a.nav-item(id="button_lk" href="/lk") Личный кабинет
        .col-4.user-block
          .img-avatar-header
            img(name="img_user" id="header_user_img" src="/images/default.jpg").img-fluid
          span(name="user_name", id="user_name_header")
          button.btn.btn-light(id="exit_button") Выход